#pragma once

#include <vector>
#include <iostream>
#include <GLFW/glfw3.h>

// class Skybox {
//     private:
//         GLuint m_VAO = 0, m_VBO = 0, m_Texture = 0;
//         GLuint loadCubemap(const std::vector<std::string>& faces);

//     public:
//         void InitializeSkybox(const std::vector<std::string>& faces);
//         void DrawSkybox();
// }